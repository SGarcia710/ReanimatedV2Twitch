import { Home, Transitions } from '../screens';

const SCREENS = [
  {
    name: 'Home',
    component: Home,
    title: 'Index',
  },
  {
    name: 'Transitions',
    component: Transitions,
    title: 'Transitions',
  },
];

export default SCREENS;
